


<div class='search-background'>

  <div class='search'>
    <h1> OpenClinical</h1>
    {{#power-select-multiple options=model.phaseOptions selected=model.selectedPhases placeholder="Filter by Phase" onchange=(action (mut model.selectedPhases )) as |phase|}}
      {{phase}}
    {{/power-select-multiple}}
    <button class='btn btn-primary' {{action 'searchStudies' }}> Search </button>
      {{search}}
    </div>
</div>

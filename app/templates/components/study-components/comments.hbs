<h3> Comments </h3>
<div class='newComment'>
  {{#if isAuthenticated}}
    {{study-components/comment-form newComment='newComment' study=study}}
  {{/if}}
</div>

<div class='comments'>
  {{#each sortedComments as |comment|}}
    {{study-components/comment comment=comment editComment= 'editComment' deleteComment='deleteComment'}}
  {{/each}}
</div>

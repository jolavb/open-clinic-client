
import { validateExists } from 'open-clinic-client/helpers/validate-exists';
import { module, test } from 'qunit';

module('Unit | Helper | validate exists');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = validateExists([42]);
  assert.ok(result);
});



import { checkTitle } from 'open-clinic-client/helpers/check-title';
import { module, test } from 'qunit';

module('Unit | Helper | check title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkTitle([42]);
  assert.ok(result);
});

